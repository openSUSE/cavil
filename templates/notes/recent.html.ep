% layout 'default';
% title 'Recent Notes';

<div id="recent-notes"></div>

% content_for 'ready_function' => begin
  % my $can_see_lawyer_only = current_user_has_role('admin', 'lawyer') ? 'true' : 'false';
  cavil.setupRecentNotes(<%= $can_see_lawyer_only %>);
% end
